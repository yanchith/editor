I spent a better half of a day debugging something weird.

How this looks like is that in a debugger I inspect a return value for a procedure and it says one thing. After the debugger jumps out of the function, the return value (after being assigned to a local variable) reports a completely different value. This happens for different functions in different places depending on how I mutate the code.

It doesn't happen all the time, only after I do something specific in the program, so I am assuming I did something super dumb elsewhere (e.g. stomped the stack).

So far, I was unable to reproduce this on the side. How would you debug something like this? Are there any sanitizers I can turn on?

---

For completeness, this is one of the functions. `view` is completely reasonable inside the function, but after being returned, it becomes null+garbage count.

```
gap_buffer_get_bytes_before_gap :: (gb: Gap_Buffer) -> [] u8 {
    assert_valid(gb);

    view: [] u8;
    view.count = gb.gap_index;
    view.data = gb.buffer.data;

    return view;
}
```
